{
    "lr": 1e-6,

    "entropy_bonus": 1e-5,

    "num_epochs": 200000,
    "time_limit": 3600,
    "num_episodes_per_epoch": 20,
    "num_nodes_per_epoch": 2000,
    "sample_rate": 0.5,

    "batch_size": 32,
    "num_agents": 25,
    "episodes_preemption": 1,

    "num_valid_instances": 100,
    "num_valid_seeds": 2,
    "validate_every": 100,
    "heuristics_during_training" : false,
    "heuristics_during_validation" : false,
    "gpu": 0,
    "seed": 0
}
